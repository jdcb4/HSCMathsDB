import ReactDOM from "react-dom/client";
import "../styles/index.css";
import { RuntimeRoot } from "./RuntimeRoot";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(<RuntimeRoot />);
